{
  "name": "react-available-times",
  "version": "1.0.0",
  "description": "A calendar to pick available time slots",
  "main": "dist/main.js",
  "repository": "git@github.com:trotzig/react-available-times.git",
  "author": "Henric Trotzig <henric.trotzig@gmail.com>",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && rollup src/main.js --format cjs --output dist/main.js",
    "build-test": "rollup src/test.js --format iife --output dist/test.js"
  },
  "devDependencies": {
    "rimraf": "^2.6.1",
    "rollup": "^0.41.6"
  }
}
